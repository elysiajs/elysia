# AGENTS.md

Agent instructions for this repository.
Generated by [sourcebook](https://github.com/maroondlabs/sourcebook). Review and edit — the best context comes from human + machine together.

## Commands

- **Dev:** `bun run --watch example/a.ts`
- **Build:** `rm -rf dist && bun build.ts`
- **Test:** `bun run test:functionality && bun run test:types && bun run test:node`

## Constraints

These constraints MUST be followed when modifying this codebase:

- **Project structure:** This is a publishable library, not an application. Focus changes on the public API surface. Avoid breaking changes to exported types and function signatures.
- **Core modules:** Hub files (most depended on): src/types.ts (imported by 17 files); src/utils.ts (imported by 14 files); src/context.ts (imported by 12 files); src/cookies.ts (imported by 11 files); src/error.ts (imported by 11 files). Changes here have the widest blast radius.
- **Circular dependencies:** Circular import chains detected: sucrose.ts → utils.ts; context.ts → server.ts → types.ts → index.ts; error.ts → schema.ts → index.ts. Avoid adding to these cycles.

## Stack

TypeScript

## Core Modules (by structural importance)

- `src/types.ts`
- `src/utils.ts`
- `src/error.ts`
- `src/context.ts`
- `src/cookies.ts`

## Conventions

- **Import conventions:** Project uses barrel exports (index.ts files that re-export). Import from the directory, not from deep paths.

## Additional Context

- Source code lives in src/ but some code files exist at the root. Keep application code in src/.
- Project uses custom error classes. Throw specific error types instead of generic Error.
- Uses TypeBox (`@sinclair/typebox`) for validation via Elysia's `t` schema builder. Follow this pattern for new schemas.

## What to Add Manually

The most valuable context is what only you know. Add:

- Architectural decisions and why they were made
- Past incidents that shaped current conventions
- Deprecated patterns to avoid in new code
- Domain-specific rules or terminology
- Environment setup beyond what .env.example shows
