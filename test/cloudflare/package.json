{
	"name": "elysia-cf",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"gen": "node gen-node.mjs",
		"test": "node gen-node.mjs && bun script/smoke.ts",
		"build:standalone": "bun build-standalone.mjs",
		"test:standalone": "bun build-standalone.mjs && bun script/smoke.ts 8801 --no-bundle -c wrangler.standalone.jsonc",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"esbuild": "0.25.4",
		"typescript": "^5.8.2",
		"wrangler": "^4.40.2"
	},
	"dependencies": {
		"elysia": "../../"
	},
	"overrides": {
		"esbuild": "0.25.4"
	}
}
